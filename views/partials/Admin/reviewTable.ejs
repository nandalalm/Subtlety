<% 
    const reviewList = typeof reviews !== 'undefined' ? reviews : [];
    const currPg = typeof currentPage !== 'undefined' ? parseInt(currentPage) : 1;
    const pgLimit = typeof limit !== 'undefined' ? parseInt(limit) : 10;
%>

<div class="table-container-shared card border-0 shadow-sm admin-cards-responsive" id="reviewTableContainer" style="background: transparent;">
    <% if (reviewList.length === 0) { %>
        <div class="text-center py-5 bg-white rounded shadow-sm">
            <i class="fas fa-search fa-3x text-muted mb-3"></i>
            <p class="text-muted">No results found for the given criteria.</p>
        </div>
    <% } else { %>
        <!-- Desktop Table -->
        <table class="table table-hover shared-table-desktop mb-0 bg-white">
            <thead class="thead-light">
                <tr>
                    <th style="width: 5%;">S.No</th>
                    <th style="width: 20%;">Reviewer</th>
                    <th style="width: 20%;">Product</th>
                    <th style="width: 15%;">Rating</th>
                    <th style="width: 20%;">Review</th>
                    <th style="width: 10%;">Date</th>
                    <th style="width: 10%;">Status</th>
                    <th style="width: 18%;" class="text-center">Action</th>
                </tr>
            </thead>
            <tbody>
                <% reviewList.forEach((review, index) => { %>
                <tr data-row-id="<%= review._id %>">
                    <td><%= (currPg - 1) * pgLimit + (index + 1) %></td>
                    <td>
                        <div class="font-weight-bold text-dark"><%= review.userId ? review.userId.firstname : 'Deleted User' %></div>
                        <div class="small text-muted"><%= review.userId ? review.userId.email : '' %></div>
                    </td>
                    <td><div class="font-weight-bold text-dark"><%= review.productId ? review.productId.name : 'Deleted Product' %></div></td>
                    <td>
                        <div class="star-rating text-warning">
                            <% for(let i=1; i<=5; i++) { %>
                                <i class="<%= i <= review.rating ? 'fas' : 'far' %> fa-star"></i>
                            <% } %>
                        </div>
                    </td>
                    <td>
                        <% const commentPreview = review.comment && review.comment.length > 20 ? `${review.comment.slice(0, 20)}...` : (review.comment || 'No review'); %>
                        <div class="small text-muted text-truncate-2" title="<%= review.comment %>">
                            <%= commentPreview %>
                        </div>
                    </td>
                    <td class="small text-muted">
                        <%= new Date(review.createdAt).toLocaleDateString('en-GB') %>
                    </td>
                    <td>
                        <span class="status-box status-<%= review.isListed ? 'successful' : 'failed' %>">
                            <%= review.isListed ? 'Listed' : 'Unlisted' %>
                        </span>
                    </td>
                    <td class="text-center">
                        <div class="d-flex align-items-center justify-content-center">
                            <a href="/admin/reviews/<%= review._id %>/view?page=<%= currPg %>&search=<%= encodeURIComponent(typeof search !== 'undefined' ? search : '') %>&sort=<%= typeof sort !== 'undefined' ? sort : 'latest' %>&rating=<%= typeof rating !== 'undefined' ? rating : '' %>"
                               class="btn btn-sm rounded-pill px-3 mr-2 table-action-btn table-btn-view">
                                View
                            </a>
                            <button class="btn btn-sm toggle-review-btn rounded-pill px-3 table-action-btn <%= review.isListed ? 'table-btn-unlist' : 'table-btn-relist' %>" 
                                    data-id="<%= review._id %>" 
                                    data-status="<%= review.isListed %>"
                                    onclick="toggleReviewStatus(this)"
                                    style="min-width: 85px;">
                                <%= review.isListed ? 'Unlist' : 'Relist' %>
                            </button>
                        </div>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>

        <!-- Mobile Cards -->
        <div class="shared-cards-mobile px-0">
            <% reviewList.forEach((review, index) => { %>
            <div class="shared-item-card mb-3" data-card-id="<%= review._id %>">
                <div class="card-header-flex">
                    <span class="font-weight-bold text-dark">#<%= (currPg - 1) * pgLimit + (index + 1) %></span>
                    <span class="status-box status-<%= review.isListed ? 'successful' : 'failed' %>" id="status-badge-mobile-<%= review._id %>">
                        <%= review.isListed ? 'Listed' : 'Unlisted' %>
                    </span>
                </div>
                <div class="card-body p-0">
                    <div class="mb-2">
                        <span class="text-warning">
                            <% for(let i=1; i<=5; i++) { %>
                                <i class="<%= i <= review.rating ? 'fas' : 'far' %> fa-star"></i>
                            <% } %>
                        </span>
                    </div>
                    <p><strong>Product:</strong> <%= review.productId ? review.productId.name : 'Deleted Product' %></p>
                    <p><strong>By:</strong> <%= review.userId ? review.userId.firstname : 'Deleted User' %></p>
                    <% const mobileCommentPreview = review.comment && review.comment.length > 20 ? `${review.comment.slice(0, 20)}...` : (review.comment || 'No review'); %>
                    <p><strong>Review:</strong> <span class="text-muted small"><%= mobileCommentPreview %></span></p>
                    <p class="small text-muted mb-0"><%= new Date(review.createdAt).toLocaleDateString('en-GB') %></p>
                    
                    <div class="mt-4 row">
                        <div class="col-12 mb-2">
                            <a href="/admin/reviews/<%= review._id %>/view?page=<%= currPg %>&search=<%= encodeURIComponent(typeof search !== 'undefined' ? search : '') %>&sort=<%= typeof sort !== 'undefined' ? sort : 'latest' %>&rating=<%= typeof rating !== 'undefined' ? rating : '' %>"
                               class="btn btn-sm btn-block rounded-pill table-action-btn table-btn-view">
                                View
                            </a>
                        </div>
                        <div class="col-12">
                        <button class="btn toggle-review-btn btn-sm btn-block rounded-pill px-4 table-action-btn <%= review.isListed ? 'table-btn-unlist' : 'table-btn-relist' %>" 
                                data-id="<%= review._id %>" 
                                data-status="<%= review.isListed %>"
                                onclick="toggleReviewStatus(this)">
                            <%= review.isListed ? 'Unlist' : 'Relist' %>
                        </button>
                        </div>
                    </div>
                </div>
            </div>
            <% }) %>
        </div>
    <% } %>
    
        <!-- Pagination moved inside for AJAX refresh support -->
        <% if (typeof totalPages !== 'undefined' && totalPages > 1) { %>
        <div class="px-3 pb-3">
            <%- include('../Common/pagination') %>
        </div>
        <% } %>
</div>
